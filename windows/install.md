Install chrome

Run win11debloat using config in here

move ssh keys

install nvidia app and update graphics drivers

winget install things
```
winget install --id Microsoft.PowerShell --source winget
winget install -e --id mcmilk.7zip-zstd
winget install --id Git.Git -e --source winget
winget install Google.Chrome
winget install --id Microsoft.VisualStudioCode
winget install -e --id magic-wormhole.magic-wormhole
winget install --id Microsoft.PowerToys --source winget
winget install -e --id Spotify.Spotify
winget install Schniz.fnm
winget install --id lsd-rs.lsd
winget install starship
```

same starship config as mac

install rust

install starship
install chocolatey
install zoxide

install llvm and gcc
```
choco install mingw llvm -y
```

choco install nerd-fonts-firacode

install hoyo launcher

install noob server

install wsl
install docker
